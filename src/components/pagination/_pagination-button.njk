{% macro paginationButton(item, current_item) %}
  {% set is_current = item !== current_item %}

  {% if is_current %}
    <a
      href="javascript:void(0);"
      class="usa-pagination__button"
      aria-label="Page {{ item }}">
      {{ item }}
    </a>
  {% else %}
    <span
      class="usa-pagination__button usa-current">
      {{ item }}
    </span>
  {% endif %}
{% endmacro %}
