{% import "./_primary-menu-macro.twig" as menus %}

<button class="usa-nav__close">
  <img src="{{ CloseIcon }}" role="img" alt="{{ nav.close.label }}">
</button>

{{ menus.menu_links(nav.items, 0, megamenu) }}
