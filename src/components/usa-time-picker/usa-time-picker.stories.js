import Component from "./usa-time-picker.twig";

export default {
  title: "Components/Form Elements/Time Picker",
};

const Template = (args) => Component(args);

export const TimePicker = Template.bind({});
