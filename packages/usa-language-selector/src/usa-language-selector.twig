<button class="usa-language-selector{{ focus ? " usa-focus" }}"{% if role %} role="{{ role }}" {% endif %}>
	<span lang="{{ lang }}" xml:lang="{{ lang }}">{{ label | default('Español') }}</span>
</button>
