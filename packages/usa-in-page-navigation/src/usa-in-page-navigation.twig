{% macro inpagenav_list(items, base_class) %}

	{% import _self as inpagenav %}

	{% set list_item_class = 'usa-in-page-navigation__item' %}
	{% set current_class = 'usa-current' %}
	{% set placeholder_link = 'javascript:void(0);' %}


	{% if heading %}
		<p class="site-subheading">{{ heading }}</p>
	{% endif %}

	{% if items %}
		<ul class="{{ base_class }}">
			{% for item in items %}
				<li class="{{ list_item_class }}">
					<a href="#section_{{ item.id }}" {% if item.current %} class="usa-current" {% endif %}>{{ item.label }}</a>
				</li>
			{% endfor %}
		</ul>
	{% endif %}
{% endmacro %}

{% import _self as inpagenav %}

<div id="in-page-navigation" class="in-page-navigation">
	{{ inpagenav.inpagenav_list(items, 'usa-in-page-navigation') }}
</div>
