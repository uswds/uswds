<div class="usa-prose measure-5 padding-2 border-1px border-gray-10 margin-bottom-4">
  <p class="font-body-lg text-bold margin-top-0">Test if the data-heading-selector attribute pulls the correct headers into the component</p>
  <p>Use the story controls to select which headers should be pulled into the component. For example:
  <ul>
    <li>Selecting "Default" should pull h2 and h3 headers into the in-page navigation.</li>
    <li>Selecting "All" should pull all headers (h1-h6) into the in-page navigation.</li>
    <li>Selecting "h4" should pull only h4 headers into the in-page navigation.</li>
    <li>Selecting "Empty value" should  pull same headers as default.</li>
    <li>Selecting "Error in value" should show nothing in the component.</li>
  </ul>
</div>

<div class="usa-in-page-nav-container">
  <aside class="usa-in-page-nav"
    {% if includeHeaders == "All" %}
      data-heading-selector="h1 h2 h3 h4 h5 h6"
      {% elseif includeHeaders == "Default value" %}
      {% elseif includeHeaders == "h1" %}
        data-heading-selector="h1"
      {% elseif includeHeaders == "h2" %}
        data-heading-selector="h2"
      {% elseif includeHeaders == "h3" %}
        data-heading-selector="h3"
      {% elseif includeHeaders == "h4" %}
        data-heading-selector="h4"
      {% elseif includeHeaders == "h5" %}
        data-heading-selector="h5"
      {% elseif includeHeaders == "h6" %}
        data-heading-selector="h6"
      {% elseif includeHeaders == "Empty value" %}
        data-heading-selector=""
      {% elseif includeHeaders == "Error in value" %}
        data-heading-selector="j1"
    {% endif %} >
  </aside>
  <main class="usa-prose">
    <h1>H1 heading</h1>
    <h2>H2 heading</h2>
    <h3>H3 heading</h3>
    <h4>H4 heading</h4>
    <h5>H5 heading</h5>
    <h6>H6 heading</h6>
  </main>
</div>
