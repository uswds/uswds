{
    "ignorePatterns" : [
        {
          "pattern": "^https://www.npmjs.com"
        },
        {
          "pattern": "^https://www.ssa.gov"
        }
    ],
    "replacementPatterns": [
        {
            "pattern": "^/",
            "replacement": "{{BASEURL}}/"
        }
  ]
}
