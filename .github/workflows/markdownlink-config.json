{
    "ignorePatterns" : [
        {
          "pattern": "^https://npmjs.com"
        },
        {
          "pattern": "^https://ssa.gov"
        }
    ],
    "replacementPatterns": [
        {
            "pattern": "^/",
            "replacement": "{{BASEURL}}/"
        }
  ]
}
